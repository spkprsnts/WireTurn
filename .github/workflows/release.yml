name: Build and Release

on:
  push:
    branches:
      - 'unstable'
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew assembleRelease
        env:
          # In GitHub Actions, the NDK is pre-installed at this path
          NDK_PATH: ${{ env.ANDROID_HOME }}/ndk-bundle

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: wireturn-release-apk
          path: app/build/outputs/apk/release/*.apk

      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: app/build/outputs/apk/release/*.apk
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Update Unstable Release
        uses: softprops/action-gh-release@v2
        if: github.ref == 'refs/heads/unstable'
        with:
          tag_name: unstable-latest
          name: "Unstable Build (Development)"
          body: "Latest build from unstable branch. Commit: ${{ github.sha }}"
          files: app/build/outputs/apk/release/*.apk
          prerelease: true
          make_latest: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
