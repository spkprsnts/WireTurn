<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.wireturn" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.wireturn" parent="Base.Theme.wireturn" />

    <!-- Splash screen theme (Core Splash Screen API) -->
    <style name="Theme.wireturn.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.wireturn</item>
    </style>
</resources>

